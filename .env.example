# Cloudflare Turnstile (contact form)
# Site key is also in .env.production for Vercel builds (public, safe to commit).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your_site_key_here

# Required on Vercel (Project Settings → Environment Variables). Never commit this.
TURNSTILE_SECRET_KEY=your_secret_key_here
