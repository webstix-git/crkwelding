/** Turnstile site key is public (embedded in the browser). Override via env if needed. */
export const TURNSTILE_SITE_KEY =
  process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY ?? "0x4AAAAAADciPSK19Dubv7Dc";
