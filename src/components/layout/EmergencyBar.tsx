import { Phone } from "lucide-react";

const EmergencyBar = () => {
  return (
    <div className="bg-primary text-primary-foreground py-2 px-4 z-50 relative">
      <div className="container mx-auto flex items-center justify-center text-xs sm:text-base font-ui tracking-wider whitespace-nowrap">
        24/7 EMERGENCY &amp; AFTER-HOURS SERVICE
      </div>
    </div>
  );
};

export default EmergencyBar;
